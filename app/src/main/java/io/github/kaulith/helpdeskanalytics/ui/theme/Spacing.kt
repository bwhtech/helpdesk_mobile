package io.github.kaulith.helpdeskanalytics.ui.theme

import androidx.compose.ui.unit.dp

// M3 4dp-grid spacing. Only the tokens actually consumed remain; older aliases
// and unused size scales (CornerRadius, IconSize) were removed during P2 audit.
object Spacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 12.dp
    val base = 16.dp
    val lg = 24.dp
    val xl = 32.dp
    val xl2 = 40.dp
}
